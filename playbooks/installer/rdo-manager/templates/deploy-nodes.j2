#!/bin/bash
export NODES_JSON=instackenv.json
export CONTROLSCALE={{ installer.nodes.controller.scale | default('1') }}
export COMPUTESCALE={{ installer.nodes.compute.scale | default('1') }}
export BLOCKSTORAGESCALE={{ installer.nodes.blockstorage.scale | default('0') }}
export SWIFTSTORAGESCALE={{ installer.nodes.swiftstorage.scale | default ('0') }}
export CEPHSTORAGESCALE={{ installer.nodes.cephstorage.scale | default ('1') }}
export CONTROLFLAVOR={{ installer.nodes.controller.flavor | default('baremetal') }}
export COMPUTEFLAVOR={{ installer.nodes.compute.flavor | default('baremetal') }}
export BLOCKSTORAGEFLAVOR={{ installer.nodes.blockstorage.flavor | default('baremetal') }}
export SWIFTSTORAGEFLAVOR={{ installer.nodes.swiftstorage.flavor | default('baremetal') }}
export CEPHSTORAGEFLAVOR={{ installer.nodes.cephstorage.flavor | default ('baremetal') }}