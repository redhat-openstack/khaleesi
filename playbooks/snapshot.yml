---
- name: Snapshot the environment
  hosts: local
  gather_facts: False
  vars:
    wait_snapshot: "{{ wait_snapshot }}"
    wait_for: "{{ wait_for }}"
  roles:
    - { role: snapshot }
  tags:
    - snapshot
