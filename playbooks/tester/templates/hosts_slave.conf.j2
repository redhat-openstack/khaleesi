{% for host in groups.openstack_nodes %}
{{ hostvars[host].ansible_ssh_host }} ansible_ssh_user=fedora
{% endfor %}

[slave]
{% for host in groups.openstack_nodes %}
{{ hostvars[host].ansible_ssh_host }} ansible_ssh_user=fedora
{% endfor %}

