parameters:
  os_user: admin
  os_password: {{ get_admin_password_result.stdout }}
  os_tenant: admin
  os_auth_url: {{ get_auth_url_result.stdout }}
  bmc_flavor: m1.small
  bmc_image: {{ provisioner.image.name }}
  baremetal_flavor: baremetal
  baremetal_image: empty
  key_name: {{ tmp.node_prefix }}default
  private_net: {{ tmp.node_prefix }}private
  bmc_prefix: {{ tmp.node_prefix }}bmc
  baremetal_prefix: {{ tmp.node_prefix }}baremetal
  node_count: {{ provisioner.node_count }}
  # virtual-baremetal-specific params ignored by quintupleo.yaml
  public_net: {{ tmp.node_prefix }}public
  provision_net: {{ tmp.node_prefix }}provision
  # QuintupleO-specific params ignored by virtual-baremetal.yaml
  undercloud_name: undercloud
  undercloud_image: {{ provisioner.image.name }}
  undercloud_flavor: m1.large
  external_net: {{ installer.network.name }}
  public_net_name: public
  provision_net_name: provision
