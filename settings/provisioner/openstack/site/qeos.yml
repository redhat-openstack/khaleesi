provisioner:
    auth_url: !lookup private.provisioner.qeos.auth_url

    network:
        type: nova
        name: public
        use_floating_ip: true

defaults:
    tenant: rhos-jenkins
    user: rhos-jenkins
