workaround:
    rhbz1278181:
        desc: '55-heat-config shouldn't use /var/run for it's DEPLOYED_DIR'
        enabled: True
    rhbz1272347:
        desc: 'director stack update 7.0 to 7.1 KeystoneAdminApiNetwork change causes unwanted services restart'
        enabled: True
