# For manual provisioning (without beaker) of osp-d virt-host setup, there must be entry point defined here
# host0 ansible_ssh_host=my.baremetal.redhat.com  ansible_ssh_user=root ansible_ssh_private_key_file=~/.ssh/id_rsa

[local]
localhost ansible_connection=local
