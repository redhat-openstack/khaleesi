---
tempest_ip: "{{ hostvars[nodes.tempest.name].ansible_default_ipv4.address }}"
tempest_default_ip: "{{ hostvars[nodes.tempest.name].tempest_default_ip }}"
tempest_private_ip: "{{ hostvars[nodes.tempest.name].tempest_private_ip }}"
