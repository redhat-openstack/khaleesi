---
repos_to_remove: ['rdo-release.repo', 'puppetlabs.repo', 'foreman.repo']

repo_production: "{{ rdo[config.version].production.repo[ansible_distribution][ansible_distribution_version] }}"
repo_current: "{{ rdo[config.version][config.repo].repo[ansible_distribution][ansible_distribution_version] }}"
