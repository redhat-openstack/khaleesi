---
  - name: answer-file nova config
    lineinfile: dest={{ packstack.answer_file }} regexp='{{ item.option }}=.*' line='{{ item.option }}={{ item.value }}'
    with_items:
      aio_nova_config_options
