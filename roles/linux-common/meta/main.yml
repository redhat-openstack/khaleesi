---
dependencies:
    - { role: linux/rhel, when: ansible_distribution == 'RedHat' }
    - { role: linux/fedora, when: ansible_distribution == 'Fedora' }
    - { role: linux/centos, when: ansible_distribution == 'CentOS' }
    - { role: linux/ntp}
